# gpt-anthropic-harness

GPT lineage; Claude Agent SDK with Claude models only. Read `../rules.md` before
working. Only this swimlane is writable for this assignment; no git commands.

## Bar and scope

Task 1 only: a useful hello-world prototype whose failures remain recorded.
This is the NIMOI persistent testbed, not a production release.

## Current state

`task-1-hello/hello.py` implements a one-turn greeting. Python syntax passed;
dependency installation failed three times with an InvalidChunkLength transport
error. No live Claude request has been made. See `task-1-hello/README.md` for usage and
`mem/task-1-record.md` for assumptions, cross-lane evidence, and failures.

Stop after task 1 for human direction; task 2 is not authorized by this assignment.
